package org.purpleBean.kmip.{{pdot}};

import org.junit.jupiter.api.DisplayName;
import org.purpleBean.kmip.*;
import org.purpleBean.kmip.api.*;
import org.purpleBean.kmip.model.core.enumeration.*;
import org.purpleBean.kmip.model.core.structure.*;
import org.purpleBean.kmip.model.core.type.*;
import java.math.BigInteger;
import java.nio.ByteBuffer;
import java.time.OffsetDateTime;
import java.time.ZoneOffset;
import org.purpleBean.kmip.test.BaseKmipTest;
import org.purpleBean.kmip.test.suite.KmipStructureTestSuite;

import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;

@DisplayName("{{STRUCTURE_NAME}} Domain Tests")
class {{STRUCTURE_NAME}}Test extends BaseKmipTest implements KmipStructureTestSuite<{{STRUCTURE_NAME}}> {

    @Override
    protected void setupDefaultSpec() {
        defaultSpec = KmipSpec.UnknownVersion;
    }

    @Override
    public Class<{{STRUCTURE_NAME}}> type() {
        return {{STRUCTURE_NAME}}.class;
    }

    @Override
    public {{STRUCTURE_NAME}} createDefault() {
        // TODO: Create a default instance of the structure
        return new {{STRUCTURE_NAME}}();
    }

    @Override
    public EncodingType expectedEncodingType() {
        return EncodingType.STRUCTURE;
    }

    @Override
    public int expectedMinComponentCount() {
        // TODO: Set the expected minimum number of components
        return 0;
    }

    @Override
    public void validateComponents(List<KmipDataType> values) {
        // TODO: Validate the components of the structure
        // assertThat(values).hasSize(0);
    }
}
