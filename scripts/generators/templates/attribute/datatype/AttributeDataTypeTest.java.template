package org.purpleBean.kmip.{{pdot}};

import org.junit.jupiter.api.DisplayName;
import org.purpleBean.kmip.api.EncodingType;
import org.purpleBean.kmip.api.KmipSpec;
import org.purpleBean.kmip.model.core.enumeration.State;
import org.purpleBean.kmip.test.suite.AbstractKmipDataTypeAttributeTestSuite;

import java.time.OffsetDateTime;
import java.time.ZoneOffset;
import java.time.ZoneOffset;
import java.math.BigInteger;
import java.nio.ByteBuffer;
import java.time.OffsetDateTime;
import java.time.ZoneOffset;

@DisplayName("{{ATTRIBUTE_NAME}} Domain Tests")
class {{ATTRIBUTE_NAME}}Test extends AbstractKmipDataTypeAttributeSuite<{{ATTRIBUTE_NAME}}> {

    // TODO: Adjust FIXED_VALUE based on DATA_TYPE
    private static final {{DATA_TYPE}} FIXED_VALUE = {{DEFAULT_VALUE}};

    @Override
    protected void setupDefaultSpec() {
        defaultSpec = KmipSpec.V1_2; // TODO: Adjust default spec if needed
    }

    @Override
    protected Class<{{ATTRIBUTE_NAME}}> type() {
        return {{ATTRIBUTE_NAME}}.class;
    }

    @Override
    protected {{ATTRIBUTE_NAME}} createDefault() {
        return {{ATTRIBUTE_NAME}}.of(FIXED_VALUE);
    }

    @Override
    protected EncodingType expectedEncodingType() {
        return EncodingType.{{ENCODING_TYPE}};
    }

    @Override
    protected boolean expectAlwaysPresent() {
        return false; // TODO: Adjust as needed
    }

    @Override
    protected boolean expectServerInitializable() {
        return true; // TODO: Adjust as needed
    }

    @Override
    protected boolean expectClientInitializable() {
        return true; // TODO: Adjust as needed
    }

    @Override
    protected boolean expectClientDeletable() {
        return false; // TODO: Adjust as needed
    }

    @Override
    protected boolean expectMultiInstanceAllowed() {
        return false; // TODO: Adjust as needed
    }

    @Override
    protected State stateForServerModifiableTrue() {
        return new State(State.Standard.PRE_ACTIVE); // TODO: Adjust as needed
    }

    @Override
    protected State stateForServerModifiableFalse() {
        return new State(State.Standard.ACTIVE); // TODO: Adjust as needed
    }

    @Override
    protected State stateForClientModifiableTrue() {
        return new State(State.Standard.PRE_ACTIVE); // TODO: Adjust as needed
    }

    @Override
    protected State stateForClientModifiableFalse() {
        return new State(State.Standard.ACTIVE); // TODO: Adjust as needed
    }
}
